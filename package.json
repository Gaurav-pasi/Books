{
  "name": "private-library",
  "version": "1.0.0",
  "description": "Password-protected, AES-256 encrypted book library hosted on GitHub Pages",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/encrypt.mjs",
    "encrypt": "node scripts/encrypt.mjs",
    "serve": "npx --yes http-server -p 8080 -c-1",
    "test": "node scripts/test-paginate.mjs && node scripts/test-app-smoke.mjs",
    "audit": "node scripts/audit-book.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "marked": "^12.0.0",
    "page-flip": "^2.0.7"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
